---
title: "Modulo Datatable"
date: "2023-10-01"
slug: "modulo-datatable"
description: ""
menu_position: 3
---

import DataTb from "../modules/datatb.js"

`<DataTb />` component can be used in MDX pages to display a dynamic data table, which are fully customisable.

## Parameters

- `path2cvs` (**required** if `dTable` id not provided): relative or full path to a csv file to use for the data Table creation
- `dEndPoint` (**required** is `path2csv` or `dTable` are not defined): full path to a Directus endpoint, complete with table name
- `dToken` (**required** if `dEndPoint` is provided): Directus access token, required if table is not public. If not proved GATSBY_DIRECTUS_TOKEN environment variable will be used
- `dTable` (**required**, if `path2csv` or `dEndPoint` are not provided): name of the Directus table containing geographical data
- `columns` (**required**): list of database columns and fields

## Examples

### Directus data source

<div className="row">
  <div className="col-sm-4">
    ```
    <DataTb
      striped={true}
      dEndPoint="https://landscapearchaeology.eu/db/items/toponimi?limit=1000&offset=0"
      dToken="sxE2XHdJMWJiymA9w2QJFGJWG9AAnq6E"
      columns={[
        {
          name: "ID luogo",
          selector: (item, i) => item.id,
          sortable: true,
        },
        {
          name: "Toponimo",
          selector: (item, i) => item.toponimi,
          sortable: true,
          cell: item => (
            <a href={`../record/?dEndPoint=${encodeURIComponent(`https://landscapearchaeology.eu/db/items/toponimi`)}&tb=toponimi&token=sxE2XHdJMWJiymA9w2QJFGJWG9AAnq6E&id=${item.id}`}>
              {item.toponimo}
            </a>
          ),
        },
        {
          name: "Provincia",
          selector: (item, i) => item.provincia,
          sortable: true,
        },
        {
          name: "Comune",
          selector: (item, i) => item.comune,
          sortable: true,
        },
      ]}
    />
    ```
  </div>
  <div className="col-sm-8">
    <DataTb
      striped={true}
      dEndPoint="https://landscapearchaeology.eu/db/items/toponimi?limit=1000&offset=0"
      dToken="sxE2XHdJMWJiymA9w2QJFGJWG9AAnq6E"
      columns={[
        {
          name: "ID luogo",
          selector: (item, i) => item.id,
          sortable: true,
        },
        {
          name: "Toponimo",
          selector: (item, i) => item.toponimi,
          sortable: true,
          cell: item => (
            <a href={`../record/?dEndPoint=${encodeURIComponent(`https://landscapearchaeology.eu/db/items/toponimi`)}&tb=toponimi&token=sxE2XHdJMWJiymA9w2QJFGJWG9AAnq6E&id=${item.id}`}>
              {item.toponimo}
            </a>
          ),
        },
        {
          name: "Provincia",
          selector: (item, i) => item.provincia,
          sortable: true,
        },
        {
          name: "Comune",
          selector: (item, i) => item.comune,
          sortable: true,
        },
      ]}
    />
  </div>
</div>

### Example with CSV data source

<div className="row">
  <div className="col-sm-4">
```
<DataTb
  path2csv="../../data/ksa.csv"
  columns={[
    {
      name: "Item Label",
      sortable: true,
      cell: item => (
            <a href={item["URI"]} target="_blank">
              {item["Item Label"]}
            </a>
          ),
    },
    {
      name: "Site Name",
      selector: row => row["Site Name"],
      sortable: true,
    },
    {
      name: "Thumbnail",
      cell: item => (
            <img src={item["Thumbnail"]} />
          ),
      sortable: true,
    },
    {
      name: "Date",
      selector: row => `${row["Early BCE/CE"]} — ${row["Late BCE/CE"]}`,
      sortable: true,
    },
    {
      name: "Collection Summary",
      selector: row => row["Collection Summary"],
      sortable: true,
    },
    
  ]}
/>
```
  </div>
  <div className="col-sm-8">

<DataTb
  path2csv="../../data/ksa.csv"
  columns={[
    {
      name: "Item Label",
      sortable: true,
      cell: item => (
            <a href={item["URI"]} target="_blank">
              {item["Item Label"]}
            </a>
          ),
    },
    {
      name: "Site Name",
      selector: row => row["Site Name"],
      sortable: true,
    },
    {
      name: "Thumbnail",
      cell: item => (
            <img src={item["Thumbnail"]} />
          ),
    },
    {
      name: "Date",
      selector: row => `${row["Early BCE/CE"]} — ${row["Late BCE/CE"]}`,
    },
    {
      name: "Collection Summary",
      selector: row => row["Collection Summary"],
    },
    
  ]}
/>

---

Data from [Kahramanmaraş Survey](https://opencontext.org/projects/ca4f4719-f2a5-4119-99fa-b04573c8929a) created by [Elizabeth Carter](http://opencontext.org/persons/3208923d-69d0-70e9-9cc1-23ad97770aec) and published on [OpenContext](https://opencontext.org/projects/ca4f4719-f2a5-4119-99fa-b04573c8929a) add distributed with [CC BY 4.0 Internatkional](https://creativecommons.org/licenses/by/4.0) license.

---

  </div>
</div>
