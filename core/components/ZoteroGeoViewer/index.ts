/**
 * ZoteroGeoViewer Component
 * 
 * Connects to a Zotero group library and visualizes items with geographical coordinates
 * on a map, providing tag-based filtering and different layout options.
 */

export { ZoteroGeoViewer } from './ZoteroGeoViewer';
export { ZoteroGeoViewerMdx } from './ZoteroGeoViewerMdx';
export type { 
  ZoteroGeoViewerProps, 
  ZoteroItem, 
  CoordinateData, 
  ZoteroGeoData, 
  LayoutType 
} from './types';